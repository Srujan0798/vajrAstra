# Level-2 Training Assets (data formatting only — no training, L2 law)

Generated by `level2/research/training_assets_gen.py` at 2026-09-13T18:41:48+00:00.
All counts computed from disk. Sources: `level2/out/` (4000 OCR packs),
`level2/reports/CER_STAGE3B.json`, `level2/pages_manifest.json`, PDF text layer
via pymupdf. Idempotent: rerun overwrites.

GT anchor: PDF text layer, dense page = >= 200 chars after NFC + lowercase +
whitespace-collapse (same basis as CER_STAGE3B.json). Dense pages: 176
(deck estimate was ~177; actual 176). Scripts: Devanagari 24, Kannada 19, Latin 43, Malayalam 8, Tamil 53, Telugu 29.

## Counts

| Asset | Lines/Pages | Deck Stage | Basis |
|---|---|---|---|
| sft_noisy_to_gold.jsonl | 1760 lines (176 dense pages x engines) | Stage 3 (SFT noisy->JSON) | engine OCR -> PDF-layer GT |
| preference_pairs_dpo.jsonl | 139 pairs | Stage 3b (SimPO/DPO) | lowest-CER chosen vs highest-CER rejected |
| disagreement_queue.json | 50 pages (top 50 of 394 rankable) | Stage 1 (layout training + human review) | median pairwise token Jaccard, ascending |

## Schemas

### sft_noisy_to_gold.jsonl — one line per (dense page x engine)
`page_id`, `script` (manifest dominant_script), `engine`, `noisy_text`
(joined pack regions[].text), `gold_text` (PDF text layer), `gold_source`
("pdf_text_layer"), `generated_at`.

### preference_pairs_dpo.jsonl — one line per dense page with >= 2 nonempty engines
`page_id`, `script`, `engine_chosen`, `engine_rejected`, `chosen_text`,
`rejected_text`, `cer_chosen`, `cer_rejected`, `margin` (cer_rejected -
cer_chosen), `gt_chars`, `generated_at`. CER labels from CER_STAGE3B.json.

### disagreement_queue.json — Stage-1 queue, ranked most-disagreed first
Top-level: `definition`, `disagreement_metric`, `queue_size`, `pages`,
`generated_at`. Per page: `rank`, `page_id`, `dominant_script`,
`mixed_book_page`, `engines_with_text`, `median_pairwise_jaccard`,
`engine_chars` (per-engine char counts), `engine_output_scripts`.

## Data quality notes (computed, not hand-written)

- GT-thin pages excluded from SFT/DPO: 224 of 400
  pages have < 200 normalized PDF-layer chars (CER_STAGE3B reason gt_thin).
- SFT lines with empty noisy_text: 9 (engine produced
  no text on a dense page; kept as honest disk truth, flagged here).
- DPO pages skipped (single engine / <2 nonempty engines): 0 / 37.
- Queue pages with <2 engines producing tokens (unrankable): 6.
